# Task 2 WIP Handoff

Plan: `docs/superpowers/plans/2026-05-06-reviewed-examples-journey.md`

Current branch intent: continue the reviewed-examples journey work on a dedicated WIP branch.

Latest completed task:
- Task 1 complete at commit `49bc3c5` (`feat(ui): rename project navigation around reviewed examples`)

Current task:
- Task 2: Documents Workbench Journey Stepper

Current base commit before this WIP handoff:
- `e5385b0` (`feat(ui): add reviewed examples journey stepper`)

Why this WIP exists:
- Task 2 passed its targeted tests once, but spec review found remaining stale cross-project state issues.
- Work in progress adds regression coverage for stale previous-project document rows and late readiness/review responses.

Known remaining work when resuming:
1. Make `DocumentListPage` journey state fully current-project scoped.
2. Prevent stale/out-of-order responses in document/readiness/review flows from re-enabling wrong journey actions.
3. Finish Task 2 verification and then rerun:
   - implementer test
   - spec review
   - code-quality review
4. Do not start Task 3 until Task 2 is fully closed.

Files already in play for Task 2:
- `frontend/src/components/ProjectJourneyStepper.tsx`
- `frontend/src/pages/DocumentList.tsx`
- `frontend/src/i18n/locales/en.json`
- `frontend/src/__tests__/project_journey_stepper.test.tsx`
- `frontend/src/__tests__/document_list.test.tsx`
- possibly small store changes in:
  - `frontend/src/stores/documents.ts`
  - `frontend/src/stores/readiness.ts`
  - `frontend/src/stores/review.ts`

Recent spec-review findings that still matter:
1. `DocumentList.tsx` used `rows.length > 0` without full project scoping, so previous-project rows could enable Extract on project switch.
2. `readiness` and `review` stores did not protect against out-of-order responses, so older requests could overwrite current-project data.

Recommended resume prompt on the next machine:

```text
Continue executing docs/superpowers/plans/2026-05-06-reviewed-examples-journey.md.

Current state:
- Task 1 is complete.
- Task 2 is partially complete and currently in WIP review-fix mode.
- Start from docs/handoffs/2026-05-07-task2-wip.md.

Requirements:
- Stay in controller mode.
- Do not start Task 3.
- Finish Task 2 by fixing stale cross-project state in DocumentList/documents/readiness/review.
- Run Task 2 tests, then spec review, then code-quality review, then commit.
```

Useful verification command:

```bash
cd frontend && npm run test -- project_journey_stepper.test.tsx document_list.test.tsx
```
